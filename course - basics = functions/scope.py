# scope = The region that a variable is recognized A variable is only available from inside the region it is created.
# and locally scoped versions of a variable can be created inside & outside functions)
# A global





def display_name(name1):
    name1 = "nigger " + "Bartekaa" # local scope
    return(name1)

name1 = "Bartek1"# global scope (available
print(display_name(name1))